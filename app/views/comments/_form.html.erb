<%= simple_form_for([@message, @message.comments.build]) do |f| %>
  <%= f.input :content, label: "Comment" %>
  <%= f.button :submit, :class => 'btn btn-default', style: 'background-color:black; color:#0a8fd5' %>
<% end %>