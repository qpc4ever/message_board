<%= simple_form_for([@message, @message.comments.build]) do |f| %>
  <%= f.input :content, label: "Comment", style: 'color:#0a8fd5;' %>
  <%= f.button :submit, class: 'btn btn-default fa fa-comment fa-2x', style: 'background-color:black; color:#0a8fd5' %>
<% end %>